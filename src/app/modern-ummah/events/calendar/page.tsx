import { ComingSoon } from '../../_components/ComingSoon';

export default function CalendarPage() {
    return (
        <ComingSoon
            title="Events Calendar"
            description="View all upcoming events and programs."
        />
    );
}
